# Dépendances épinglées (versions testées). Mettre à jour de façon contrôlée.
fastapi==0.136.3
uvicorn[standard]==0.49.0
stripe==15.2.0
fpdf2==2.8.7
# Pilote PostgreSQL — importé uniquement si DATABASE_URL est défini (production).
# En local sans DATABASE_URL, jamais chargé (base SQLite par défaut).
psycopg[binary]>=3.1,<4
