# ⚠️ SECRETS & ÉTAT LOCAL — NE JAMAIS PUBLIER
.env                    # variables de déploiement (racine)
backend/.env            # clés Stripe, mot de passe SMTP…
backend/.secret         # clé de signature des jetons
backend/voltpc.db       # base SQLite (recréée automatiquement)

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Outils / éditeur
# Ignorer .claude/ SAUF le skill projet run-voltcore (versionné, partagé entre machines).
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/run-voltcore/
.vscode/
.graphifyignore
graphify-out/
*.log
.DS_Store

# Sauvegardes locales
*.localbak
frontend/_backup_design_*/
